# Body of Evidence — Validation Script Dependencies
# Install with: pip install -r scripts/requirements.txt
#
# NOTE: versions are pinned exactly for CI reproducibility. Update
# deliberately, not incidentally.

# YAML parsing
pyyaml==6.0.3

# JSON Schema validation (draft 2020-12) + local $ref registry
jsonschema==4.26.0
referencing==0.37.0

# ULID generation (used by contributors creating new entities)
# Usage: python3 -c "from ulid import ULID; print(ULID())"
python-ulid==4.0.1

# Test runner (used by CI)
# 9.0.3 fixes GHSA-6w46-j5rx-g56g (tmpdir/tmp_path symlink-attack advisory)
pytest==9.1.1

# Coverage measurement for pytest (used by CI to produce coverage.xml for
# SonarQube Cloud's sonar.python.coverage.reportPaths; no minimum threshold
# is enforced yet — see DECISIONS.md)
pytest-cov==7.1.0
