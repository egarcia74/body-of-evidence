# CLAUDE.md — Body of Evidence

Project memory for Claude Code sessions. Keep this current when architecture or conventions change.

## What this is

Open-source, version-controlled evidence platform for transparent, reproducible investigations built from primary sources. **Pre-alpha (schema v0.2). No investigations published yet.** Structured YAML is the canonical source of truth; markdown/pages/MCP are generated views.

## Current state (2026-08-06)

- Commit `f758968`: v0.1 founding scaffold
- Commit `1bfdae7`: schema v0.2 — response to first independent review (3.5/10, no-go)
- Nine independent reviews in `docs/reviews/` (3.5 → 4.8 → 5.8 → 6.2 → 6.4 → 6.6 → 6.8 → 6.9 → 7.0/10; the ninth pass is referenced by the tenth pass's findings but not separately filed). Each pass treats the previous round's remediation claims as propositions to falsify, not evidence of completion — and EVERY pass so far has found at least one claim that was narrower than stated, including the fix for the previous pass's narrowness. Tenth pass, on D-023's H-21/H-22/M-22 fixes: behavior verified correct for the demonstrated cases, but two claims were overstated — internal (non-root) package symlinks were rejected only by `run_reference_validation`, so the other four standalone checks (`schema`, `ids`, `orphans`, `provenance`) still passed vacuously on a package containing an unmanifested internal symlink, same failure class as M-22 (M-27); and the test claiming to prove the H-21 historical-reference exemption across all 32 registered reference locations actually only exercised a CURRENT referencing entity throughout, never a historical one (M-28). Also flagged: package discovery walked each package root multiple independent times rather than once (M-24), and `CLAUDE.md` itself overstated which diagnostics were centralized (L-11). All FIXED per D-024: `boe_files.PackageDiscovery`/`discover_packages` now do ONE walk per package producing entity files, internal symlinks, and traversal errors together; `preflight_diagnostics` (replacing the separate `symlinked_root_diagnostics`/`traversal_error_diagnostics` helpers) is called by all five validators from that shared discovery, so internal-symlink rejection is no longer references-only; the historical-registry test was renamed to describe what it actually tests (current-source coverage) and a new test was added that genuinely exercises a historical source across all 32 locations. **Pattern to internalise (now confirmed across SIX consecutive rounds — D-018 through D-024):** "I fixed the specific case the reviewer demonstrated" is not the same as "I fixed the general claim" — and the tenth pass adds a new variant: a claim can be overstated not just about WHAT was fixed but about HOW UNIFIED the fix is (four validators calling the same two helper functions is not the same guarantee as "all five validators fail closed the same way," and "these functions share a traversal" must mean literally one walk, not merely one implementation called four times).
- All ADRs in `DECISIONS.md` (D-001..D-024). D-016 is the accepted DIRECTION for immutable edition manifests — design ADR still required; do not implement piecemeal. D-023's H-21 fix (carried forward by D-024) is explicitly interim, pending D-016's Edition-scoped historical resolution.
- Lint baseline is deliberately scoped and GREEN (`.markdownlint.jsonc`, `.yamllint` — rationale in the files). Keep it green; if you tighten rules, fix the whole repo in the same commit.
- **`main` now requires PRs** (branch protection, `enforce_admins: true` — the repo owner is not exempt, so even solo pushes must go through a PR; `required_approving_review_count: 0` so self-merge is still possible). Every PR gets an automatic CodeRabbit review (GitHub App, authorized on all repos); same-repository, non-Dependabot PRs also get a SonarQube Cloud scan (`.github/workflows/sonarcloud.yml`, project `egarcia74_body-of-evidence` — informational only for now, not a required check, since the codebase has no scan baseline yet). Dependabot version-update PRs (`.github/dependabot.yml`, weekly, pip + github-actions) go through CodeRabbit and lint/validate, but NOT the Sonar scan — GitHub withholds Actions secrets from Dependabot-triggered PR runs (same as fork PRs), so `sonarcloud.yml` deliberately skips them (`github.actor != 'dependabot[bot]'`) rather than failing on a missing token. Local CodeRabbit review via `/code-review` before pushing (see repo-scoped memory) is now a second, earlier check on top of the PR-triggered one — **and it is not merely a nice-to-have**: the tenth-pass review found PR #13's GitHub CodeRabbit check reported green while its own status said "Review rate limited," meaning no GitHub-app review actually ran (M-26). A green CodeRabbit check on a PR is NOT proof a review completed — before merging, confirm either the GitHub check's summary text says a review actually ran (not just "success"), or that a local `coderabbit review --agent` pass was recorded (e.g. referenced in a PR comment) as the fallback. SonarCloud's own open findings (separate from the architecture-review pipeline) are triaged per D-023: genuinely-unused params removed, interface-required ones marked accepted, cognitive-complexity refactors deliberately deferred to D-016 rather than churning `validate_references.py` twice.
- **Next major task:** the D-016 editions ADR (immutable edition manifests + RFC 8785 canonical JSON + version-pinned references, designed as one unit — resolves C-02/C-03, and is the real fix for D-023's interim H-21 rule). After that: evidence→artifact selectors (H-04), assessment graph semantics (H-03), Event/Relationship assertion bypass (H-07).

## Core invariants (do not violate)

1. **Published entity version files are never modified.** Not content, not status. New version = new file, same `id`, new `version_id`.
2. **Supersession lives in `package.yaml`** (the manifest), never in the superseded file. There is no `superseded` status. Manifests are mandatory, path-contained, and list exactly one current version per id.
3. **Polarity lives on `ClaimEvidenceLink`**, never on Evidence. Evidence has no claim references.
4. **References are single-direction** (link→claim, link→evidence, assessment→claim). Backlinks are derived, never stored.
5. **Conclusion ≠ confidence ≠ dispute status.** Three separate Assessment fields. "Contested" is not a confidence level.
6. **Confidence labels:** 1=speculative, 2=weak, 3=moderate, 4=strong, 5=near_certain. Ordinal, not probabilistic.
7. **Tier A/B sources need a SHA-256 artifact digest.** URLs locate; digests identify.
8. **IDs:** `boe:<type>:<ulid>` stable across versions; `version_id` bare ULID per version. Crockford Base32 — no I, L, O, U; first char 0–7. **Repeated stable ids across files are VALID** (that is the versioning model); version_ids are globally unique. Do not "fix" duplicate ids without checking version_ids first.
9. **Every reference made BY a CURRENT entity version must resolve to the manifest's CURRENT version of the target id** — not merely to "a file with this id exists" (D-021/H-20, the highest-severity finding across the first seven rounds). The "made BY a current entity" qualifier is load-bearing (D-023/H-21): a historical/superseded referencing file is exempt from this rule — it still gets `REF_NOT_FOUND`/`REF_TYPE_MISMATCH`/`REF_WRONG_PACKAGE` checks, just not manifest-currency — because a historical record describing what it once referenced must stay valid even after that target is later retired entirely; applying H-20 unconditionally would make keeping history at all a validation failure. This is an INTERIM rule pending D-016 (Editions should resolve historical references against the Edition that was current when the referencing file was written, not a current/not-current boolean). Manifests are parsed BEFORE ordinary reference validation, specifically so `current_maps` exists when references are checked; get this pipeline ORDER wrong again and H-20 comes back. Every field in `validate_references.py`'s `REFERENCE_FIELDS`/`NESTED_REFERENCE_FIELDS` registry is checked (D-020/H-18, D-021/M-19; this IS the complete list, enforced by a schema-scanning completeness test AND a behavioural test that dangles every entry and asserts it's actually executed — do not add a registry entry that only satisfies the completeness test), not just Revision endpoints (D-018/H-02b). A stable id CAN legitimately appear in more than one file — `id_index` is a multimap; a reference prefers a same-package match and only flags `REF_WRONG_PACKAGE` when none exists (D-020/H-17; a naive single-owner index produces false positives). There is deliberately no way yet to declare "package A depends on package B". A symlink is rejected everywhere — a package ROOT, live OR DANGLING (D-019/H-15, D-023/H-22: `validate.py`'s discovery filter is `p.is_symlink() or p.is_dir()`, not `p.is_dir()` alone, since a dangling symlink fails `is_dir()` and was previously invisible), a manifest-listed entity path (D-018/M-11), an ordinary unmanifested entity file (D-020/H-19), AND any directory or non-YAML file (D-021/M-20) — before any file is ever read, and this now holds under EVERY standalone `--check`, not just `references` (D-024/M-27: `schema`/`ids`/`orphans`/`provenance` used to pass vacuously on a package containing an unmanifested internal symlink, because `find_entity_files` already silently excludes symlinks by design and nothing told the other four checks that had happened). Traversal itself is fail-closed (D-023/M-22, generalized to one shared walk per package by D-024/M-24): an unreadable subdirectory produces a `PACKAGE_SUBTREE_UNREADABLE` diagnostic instead of being silently omitted from results, since `os.walk` without `onerror` (and `pathlib.rglob`, which has the same underlying behaviour) both swallow that failure by default.
10. **Validation must never be vacuous.** `validate.py` fails on empty runs; fixtures prove both directions with exact, duplicate-preserving diagnostic assertions (structured `Diagnostic(code, validator, path, message, location)`, compared as a sorted list not a set — D-019/M-07b closed the gap where a set silently dropped duplicate diagnostics).

## AI contribution limits

`AI_GUIDELINES.md` governs what Claude Code (and any AI tool) may do in this repo. Read it before touching investigation content. Summary: AI may draft structure, write tooling/schema code, and assist search/summarisation — but may **not** generate evidence claims, assign confidence levels, produce source citations, or have AI-drafted assessment text pass as human analysis without disclosure. This applies to investigation *content* work, not to schema/validator engineering.

## Commands

```bash
pip install -r scripts/requirements.txt   # pinned deps (needs jsonschema>=4.18 + referencing)
python3 scripts/validate.py --self-test   # valid fixtures must pass, invalid must fail
python3 scripts/validate.py               # validate investigations (fails if none exist; --allow-empty to override)
python3 -m pytest tests/ -q               # 72 tests: fixtures, exact diagnostic assertions, CLI-level integration, schema-completeness scan, registry-executability scan (REF_NOT_FOUND + REF_NOT_CURRENT, current-source AND historical-source), shared-discovery call-count proof
python3 scripts/validate.py --root <dir>  # validate an arbitrary package directory instead of <repo>/investigations (D-019/M-15; --root and its enumeration are error-handled — D-020/M-18, D-021/M-21)
python3 -c "from ulid import ULID; print(ULID())"  # generate a new version_id/id ULID
```

CI (`.github/workflows/validate-schema.yml`) runs pytest + self-test + investigations with `--allow-empty` (remove that flag when the first real investigation lands).

## Layout notes

- `schema/` — 16 JSON Schemas (draft 2020-12). `common.schema.json` holds shared defs. `package.schema.json` is the manifest.
- `fixtures/valid/harbour-tender-inquiry/` — complete fictional package; must pass every check. Contains a superseded claim version (same id, different version_id, absent from manifest) + connecting Revision — this PROVES the versioning workflow; do not "clean it up". `fixtures/invalid/*` — fourteen packages, one violated invariant each (incl. FIVE real tracked symlinks: manifest-symlink-escape's entity-path symlink, investigation-root-symlink's package-ROOT symlink, unmanifested-symlink's not-listed-in-package.yaml entity symlink, broken-unmanifested-symlink's DANGLING one, and symlinked-subdirectory's symlinked DIRECTORY — all five are SUPPOSED to be there; the dangling one is excluded from `.yamllint`'s scan on purpose, see that file). Fixtures derive from the invariants, never from current validator behaviour (the original duplicate-id fixture enshrined a wrong invariant; see D-015). `fixtures/cross_package/{pkg-a,pkg-b}` — two packages sharing a stable id on purpose, validated TOGETHER by dedicated tests (not the self-test loop, which only ever validates one package directory at a time) to prove package-scoped revision ownership (D-018/H-02b), package-scoped ordinary references (D-019/H-02c), AND that a same-package match is preferred over a stale cross-package one (D-020/H-17).
- `scripts/boe_files.py` — shared file discovery AND the `Diagnostic(code, validator, path, message, location)` type every validator returns (D-018/D-019). All validators use it (scans .yaml AND .yml, rejects duplicate YAML keys, refuses to descend into a symlinked package root OR read/traverse ANY individual symlink anywhere in a package — file, directory, or non-YAML — D-020/H-19, D-021/M-20). `PackageDiscovery`/`discover_packages` (D-024/M-24) walk EACH package root exactly ONCE, producing entity files, internal symlinks, and traversal errors together in a single `_walk_package` pass — this replaced an earlier design where `find_entity_files`, `find_all_symlinks`, and `find_traversal_errors` each walked the same tree independently, which the tenth-pass review caught both as wasted I/O and as a documentation claim ("they share one traversal") that the code didn't actually satisfy. Every `run_*_validation` function accepts an optional `discoveries` parameter and only calls `discover_packages` itself when it's omitted; `validate.py`'s `run_all_checks` (the real entry point for a `--check all` run and every `--self-test` fixture pass) builds ONE `discoveries` list and passes it to all five checks — a same-day local CodeRabbit pass on this response caught that the first cut of this fix still walked each package once PER VALIDATOR (five times per run), not once per run, which is why that parameter exists; `test_run_all_checks_walks_each_package_exactly_once` proves the full-run guarantee by counting actual `discover_package` calls. `preflight_diagnostics(discoveries, validator)` is called at the top of ALL FIVE `run_*_validation` functions, from the SAME discovery each validator already needed for its own entity iteration (`entity_files_from`/`entities_from`) — so a standalone `--check` run fails closed on a symlinked root, an internal symlink, OR an unreadable subtree, not just `run_reference_validation` (D-023/M-22, D-024/M-27: the M-22 root/traversal fix alone still let the other four checks pass vacuously on a package containing an unmanifested internal symlink, since `find_entity_files` already silently excludes symlinks by design). The lower-level `find_entity_files`/`find_all_symlinks`/`find_traversal_errors` functions remain as directly-tested primitives but are no longer called from any `run_*_validation` entry point. Any new validator must use `discover_packages`/`preflight_diagnostics` too, and accept a `discoveries` parameter so `run_all_checks` can share its walk with it.
- `scripts/validate_references.py`'s `REFERENCE_FIELDS`/`NESTED_REFERENCE_FIELDS` (D-020/H-18, made executable not just descriptive in D-021/M-19) — the single source of truth for which schema fields get reference-checked AND resolved against manifest currency (D-021/H-20), kept honest by `tests/test_validation.py::TestReferenceRegistryCompleteness`, which scans every schema for boe-ID-pattern fields and fails if the registry and the schemas disagree in either direction, PLUS a behavioural test that dangles every registry entry and asserts each one is actually checked. Adding a reference field to a schema without a matching registry entry WILL fail a test — that is the point.
- `investigations/_template/` — copy to start an investigation. Includes `package.yaml` and `links/`.

## Known open items (deliberately deferred — see DECISIONS.md end section)

Governance placeholders (CITATION.cff authors, schema `$id` domain — need real human decisions from Eddie; CODEOWNERS and security contact are fixed, see below), D-016 editions design (top of the queue), deterministic JSON release format, signed releases, SQLite/MCP query index (v0.3), structured evidence selectors + evidence→artifact pinning, SourceWork/Artifact/Fragment split, explicit cross-package dependency declarations (accidental/malicious cross-package Revision claims are now blocked per D-018, but there is still no way to intentionally DECLARE one package depends on another), controlled relationship predicates, YAML resource limits, calendar-valid dates, Actions SHA-pinning + hash-locked deps, DCO/CLA, confidential-source vault (confidential material is prohibited in the repo until one exists — now normative in CONTRIBUTING/ETHICS/templates). **Fourth-pass review also flagged now-live public-repo operational defects.** FIXED same day: CODEOWNERS now names `@egarcia74` (not `@your-org/maintainers`), GitHub private vulnerability reporting is enabled and SECURITY.md points at it, `main` is protected (Lint/Validate checks required, force-push and deletion blocked), Discussions is enabled, ordinary `your-org` repo-link placeholders are replaced with `egarcia74/body-of-evidence` in CODEOWNERS/CHANGELOG/CITATION/CONTRIBUTING/README, and Dependabot vulnerability alerts are enabled. **Fifth-pass review found the LICENSE fix had NOT actually worked** — GitHub still reported `NOASSERTION` because the text itself (not just the appended block I'd removed) was a paraphrase of Apache-2.0. Now fixed with the byte-for-byte official text (D-019/H-16) — verify via `gh api repos/egarcia74/body-of-evidence --jq .license` before ever reporting a license-detection claim as fixed again. Still open: the schema `$id` namespace (deliberately NOT substituted — needs a decision on a stable identity that survives repository transfer, not just the current GitHub owner) and CITATION.cff authors.

## Conventions

- Conventional commits (`feat:`, `fix:`, `schema:`, `docs:`; `!` for breaking).
- British/American English consistent per file (docs currently use organisation with -s).
- Fixture/example data must be fictional or safely historical; politically neutral where possible.
- When schemas change: update examples, fixtures, tests, TERMINOLOGY.md, and ARCHITECTURE.md together — the test suite catches example/schema drift.
- Remote: `origin` → `github.com/egarcia74/body-of-evidence` (public). Never push without being asked, even though the repo is already public.
